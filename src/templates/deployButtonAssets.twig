{# /**
 * CraByFy Universal plugin for Craft CMS 3.x
 *
 * @author    Dunckelfeld
 * @copyright Copyright (c) 2018 Dunckelfeld
 * @link      dunckelfeld.de
 * @package   CraByFy Universal
 * @since     1.0.0
 */
#}

{# Not sure if this is an okay approach but it works – sooo, unless someone can point me to best practice... ~seamofreality #}
<script>
    {% if crabyfyUniversalPreviewDeployStatusEndpoint is defined %}
    var crabyfyUniversalPreviewDeployStatusEndpoint = '{{ crabyfyUniversalPreviewDeployStatusEndpoint|escape('js') }}';
    {% endif %}

    {% if crabyfyUniversalLiveDeployStatusEndpoint is defined %}
    var crabyfyUniversalLiveDeployStatusEndpoint = '{{ crabyfyUniversalLiveDeployStatusEndpoint|escape('js') }}';
    {% endif %}
</script>
{% do view.registerAssetBundle("dunckelfeld\\crabyfyuniversal\\CraByFyUniversalAssets") %}
